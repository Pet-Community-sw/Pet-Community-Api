이번에 한 일
- 단순 조회/위임/매퍼 성격의 테스트를 대거 제거하고, 회귀 위험이 큰 테스트만 남겼다.
- 남긴 테스트는 위치 파이프라인, 채팅 재전송/읽음 처리, 댓글 회귀, 산책 상태 전이, Redis 값 파싱, 채팅방 프로필 배치 조회 검증이다.
- `./gradlew test`로 전체 테스트를 다시 실행해 통과를 확인했다.

현재 진행 상태
- 의미 있는 테스트만 남기도록 테스트 스위트 정리가 완료되었다.
- 현재 `src/test/java`에는 핵심 로직을 검증하는 테스트 8개만 남아 있다.

다음에 할 일
- 남겨둔 핵심 테스트가 실제 버그를 계속 잘 막는지, 기능 수정 시 테스트 이름과 의도를 함께 유지한다.
- 자동완성 캐시 역직렬화 이슈처럼 아직 분석만 끝난 항목은 별도 수정 작업으로 진행한다.

이번 세션 추가 분석
- TDD 전환을 위해 현재 테스트 공백을 다시 검토했고, 우선순위가 높은 테스트 후보를 정리했다.
- 최우선 후보는 `ProfileService.updateProfile`의 기피 견종 갱신 회귀, `ScheduleService.getSchedules`의 profile 미존재 경로, `WalkRecordService.finishWalkRecord`의 경로/거리/정리 처리, `TokenService`의 재발급·로그아웃 블랙리스트 처리, STOMP 구독 권한 검증이다.
- 이미 남아 있는 위치 파이프라인, 채팅 재전송, 읽음 처리, Redis 파싱 테스트는 그대로 유지하면 된다.

이번 세션 추가 작업
- `LocationPipeline`에서 `BackpressureStrategy.LATEST`를 제거하고 `Observable` 체인으로 단순화했다.
- `throttleFirst` 기반 이벤트 제한은 유지한 상태로 `LocationPipelineTest`, 전체 `./gradlew test` 통과를 확인했다.

이번 세션 추가 작업
- `LocationPipeline`의 timeout 및 파이프 에러를 `subscribe`의 에러 consumer에서 로그 후 `clean()` 하도록 단순화했다.
- `doOnError`, `onErrorComplete`, `onComplete` 기반 정리를 제거하고 `LocationPipelineTest`, 전체 `./gradlew test` 통과를 확인했다.

이번 세션 추가 작업
- `LocationPipeline`에서 `observeOn(Schedulers.computation())` 제거 상태를 기준으로 `LocationPipelineTest`와 전체 `./gradlew test`를 다시 검증했다.
- 두 테스트 모두 통과했고, 현재 구조에서 `onErrorResumeNext(... Observable.empty())`가 `saveAndSend` 실패 시 현재 이벤트만 드롭하고 파이프라인은 유지하는 역할임을 코드 기준으로 정리했다.

이번 세션 추가 작업
- `LocationPipeline`의 실제 실행 스레드를 확인하기 위해 초기 요청/재사용 요청 각각에 대한 스레드 추적 테스트를 추가했다.
- 검증 결과 첫 요청은 `locationInitExecutor` 스레드에서 `saveAndSend` 전 단계가 실행되고, 재사용 요청은 호출 스레드에서 `saveAndSend` 전 단계가 실행되며, `saveAndSend` 이후는 `Schedulers.io()` 스레드에서 이어짐을 확인했다.
- `./gradlew test --tests com.example.petapp.application.service.location.LocationPipelineTest`, `./gradlew test` 모두 통과했다.

이번 세션 추가 작업
- `locationInitExecutor`라는 이름이 실제 역할과 맞지 않아 `locationPipelineExecutor`로 변경하고, 스레드 이름 prefix도 `LocationPipelineThread-`로 맞췄다.
- `LocationPipeline`의 member mismatch 로그 레벨을 `warn`에서 `error`로 올렸고, 스레드 추적 테스트도 “초기화 스레드”가 아닌 “파이프라인 스레드” 의미에 맞게 정리했다.
- `./gradlew test --tests com.example.petapp.application.service.location.LocationPipelineTest`, `./gradlew test` 모두 다시 통과했다.

이번 세션 추가 작업
- `LocationPipeline.send()`의 `thenAccept`를 `thenAcceptAsync(..., locationPipelineExecutor)`로 바꿔, 재사용 요청도 STOMP 스레드가 아니라 `locationPipelineExecutor`에서 `subject.onNext()`와 앞단 필터링이 시작되도록 정리했다.
- 이에 맞춰 `LocationPipelineTest`의 재사용 요청 스레드 기대값과 테스트명을 수정했고, `./gradlew test --tests com.example.petapp.application.service.location.LocationPipelineTest`, `./gradlew test` 모두 통과했다.

이번 세션 추가 분석
- 프로젝트의 `*QueryService`, `*QueryUseCase` 구조를 확인하고 읽기 책임을 별도로 분리했을 때 얻는 장점을 정리했다.
- 대표적으로 `ChatRoomQueryService`, `ProfileQueryService`를 확인했고, 조회 전용 트랜잭션, 재사용 가능한 조회 계약, 쓰기 서비스의 책임 축소가 핵심 장점임을 확인했다.
- 추가로 Query 분리가 과할 수 있는 기준을 정리했다. 단순 repository 위임만 있고 재사용·예외 정책·캐싱·배치 조회 가능성이 없는 경우에는 클래스 수와 의존성만 늘어날 수 있다.
- 면접 답변 관점에서는 “읽기와 쓰기의 책임을 명확히 나누고, 공통 조회 정책을 재사용하며, 향후 캐싱/배치 조회/readOnly 최적화를 적용하기 쉽게 하기 위해 분리했다”는 식으로 설명하면 된다고 정리했다.
- 현재 프로젝트 기준으로는 `*QueryUseCase`와 `*QueryService`를 대부분 기존 도메인 `UseCase`/`Service`로 합치는 편이 더 단순하고 설명 가능성이 높다고 판단했다. 다만 여러 게시글 구현체가 공통으로 쓰는 `PostQueryUseCase`처럼 다형성 성격이 있는 계약은 별도 판단이 필요하다.

현재 진행 상태
- `*QueryUseCase`, `*QueryService` 분리 구조를 각 도메인의 기존 `UseCase`/`Service`로 통합했다.
- `RoleQueryUseCase`/`RoleQueryService`는 `RoleUseCase`/`RoleService`로, `PostQueryUseCase`/`PostQueryService`는 `PostUseCase`/`PostService`로 이름과 역할을 정리했다.
- `Member`, `Profile`, `ChatRoom`, `WalkRecord`, `Token`, `PetBreed`, `Like`, `Comment`, `Review`, `WalkingTogetherPost`의 조회 메서드는 기존 도메인 `UseCase`와 `Service`로 흡수했다.
- `ChatRoomService`와 `ReaderService`, `WalkRecordService`와 `LocationPipeline` 사이에 생길 수 있는 순환 의존은 `ObjectProvider` 기반 지연 조회로 끊었다.
- `rg "QueryUseCase|QueryService" src/main/java src/test/java` 결과 남은 참조가 없음을 확인했다.
- `./gradlew test` 전체 테스트 통과를 확인했다.

다음에 할 일
- 변경사항을 검토받은 뒤 커밋 메시지를 확정하고 커밋한다.

이번 세션 추가 작업
- Query 통합 변경사항의 커밋 범위를 검토했고, `debezium/data`, `loadtest/test.js`, `member_bulk*.ndjson`, `application.yml`, `.gitignore` 변경은 이번 커밋 대상에서 제외했다.
- `src/main/java`, `src/test/java`의 Query 통합 관련 변경만 스테이징했다.
- `rg "QueryUseCase|QueryService" src/main/java src/test/java` 결과 남은 참조가 없음을 다시 확인했다.
- `./gradlew test` 전체 테스트가 통과했다.

현재 진행 상태
- Query UseCase 통합 구현과 검증이 완료되었고, `refactor : Query UseCase 통합` 커밋을 생성했다.
- 커밋 해시: `6a91c79`

다음에 할 일
- 커밋 후 남아 있는 별도 변경사항(`application.yml`, 부하테스트 파일, 대용량 ndjson, `.gitignore`)은 필요 여부를 따로 판단한다.

이번 세션 추가 분석
- 이력서/포트폴리오에 작성할 JWT 로그아웃 보안 이슈와 Redis TTL 기반 블랙리스트 설계 내용을 코드 기준으로 확인했다.
- 확인한 흐름은 `AuthController.logout` -> `AuthService.logout` -> `TokenService.delete`에서 refresh token을 삭제하고 access token을 Redis 블랙리스트에 TTL로 저장하려는 구조이며, `JwtAuthenticationFilter.getToken`에서 Redis 조회로 로그아웃된 토큰을 차단하려는 구조다.
- 설계 의도는 JWT의 stateless 특성 때문에 서버가 발급 후 토큰을 직접 무효화할 수 없는 문제를 Redis TTL 기반 블랙리스트로 보완하는 것이다.
- 다만 현재 코드 기준으로는 `TokenService.blacklistAccessToken`이 `tokenCachePort.create("blacklist", accessToken, 30 * 60L)` 형태로 저장하고, 필터는 `port.exist(arr[1])`로 access token 자체를 key로 조회하고 있어 저장 key와 조회 key가 맞지 않는 개선 포인트가 있다.

현재 진행 상태
- 포트폴리오 서술에 필요한 상황, 문제, 선택지, Redis TTL 선택 이유, 구현 흐름, 해결 효과를 정리했다.
- 코드 변경이나 테스트 실행은 하지 않았다.

다음에 할 일
- 실제 기능 완성도를 높이려면 Redis key를 `blacklist:{accessToken}` 또는 access token 자체 key로 일관되게 저장/조회하도록 수정하고, 로그아웃 토큰 재사용 차단 테스트를 추가한다.

이번 세션 추가 분석
- 실무 면접 준비를 위해 `loadtest` 디렉터리의 k6 부하테스트 스크립트를 확인했다.
- 회원 검색 부하 테스트는 `member-search-load.js`에서 warmup 20초 이후 ramping-vus로 50명, 200명까지 부하를 올리고 `/members/auto-complete` API를 호출하는 구조다.
- 검색어는 hot/warm/random 키워드를 섞어 실제 자동완성처럼 반복 검색과 일부 랜덤 검색이 함께 발생하도록 구성되어 있다.
- `res.timings.duration`을 k6 Trend 지표로 기록하고, README의 Grafana 지표에서는 MySQL LIKE, Elasticsearch, Elasticsearch+Redis 캐시 구조의 p99 응답시간을 비교했다.
- 면접 답변 관점에서는 평균 응답시간이 아니라 p99를 본 이유를 “자동완성은 타이핑마다 반복 호출되므로 일부 느린 요청도 사용자 경험에 직접 영향을 주고, 평균은 tail latency를 숨길 수 있기 때문”이라고 설명하면 된다.

현재 진행 상태
- 성능 측정 방식과 p99를 선택한 이유에 대한 면접 답변 초안을 정리했다.
- 코드 변경이나 테스트 실행은 하지 않았다.

다음에 할 일
- 실제 발표 자료나 README 문구로 반영하려면 측정 환경, 데이터 건수, 부하 조건, p99 수치를 한 표로 정리하면 좋다.

이번 세션 추가 분석
- `./gradlew bootRun`으로 현재 부트 실패를 재현했다.
- 실패 원인은 Spring 빈 순환참조이며, 순환 경로는 `ChatRoomService -> ReaderService -> StompAdapter -> DelegatingWebSocketMessageBrokerConfiguration -> StompConfig -> StompInterceptor -> commandStrategyMap -> SubscribeStrategy -> ChatRoomSubscribeStrategy -> ChatRoomService`이다.
- `ReaderService`는 이미 `ObjectProvider<ChatRoomUseCase>`를 사용하지만, `ChatRoomSubscribeStrategy`가 `ChatRoomUseCase`를 생성자에서 즉시 주입받아 WebSocket 전략 초기화와 `ChatRoomService` 생성이 다시 연결되고 있다.

현재 진행 상태
- 코드 수정 전 원인 분석까지 완료했다.
- 가장 작은 수정 후보는 `ChatRoomSubscribeStrategy`의 `ChatRoomUseCase` 의존성을 `ObjectProvider<ChatRoomUseCase>`로 바꾸고 `handle()` 내부에서 조회하도록 지연시키는 것이다.

다음에 할 일
- 수정 계획을 검토받은 뒤 `ChatRoomSubscribeStrategy`만 최소 변경하고 `./gradlew bootRun`, 필요 시 `./gradlew test`로 검증한다.

이번 세션 추가 분석
- 순환참조가 어느 커밋에서 발생했는지 확인하기 위해 `6a91c79 refactor : Query UseCase 통합` 전후 diff를 확인했다.
- 해당 커밋 이전에는 `ChatRoomSubscribeStrategy`가 `ChatRoomQueryUseCase`에 의존했고, 구현체 `ChatRoomQueryService`는 `ChatRoomRepository`만 의존하는 조회 전용 빈이라 STOMP 설정과 연결되지 않았다.
- `6a91c79`에서 `ChatRoomQueryUseCase`/`ChatRoomQueryService`가 제거되고 `find`, `isExist`가 `ChatRoomUseCase`/`ChatRoomService`로 흡수되면서 `ChatRoomSubscribeStrategy -> ChatRoomUseCase -> ChatRoomService` 경로가 생겼다.
- 동시에 `ChatRoomService`는 `ReaderUseCase`를 의존하고, `ReaderService`는 `SendPort`를 통해 `StompAdapter`를 의존하므로 WebSocket 설정 초기화 경로와 다시 만나는 순환 그래프가 완성되었다.

현재 진행 상태
- 순환참조 발생 커밋은 `6a91c79 refactor : Query UseCase 통합`으로 판단된다.
- 정확한 변경 포인트는 `ChatRoomSubscribeStrategy`의 의존성이 `ChatRoomQueryUseCase`에서 `ChatRoomUseCase`로 바뀐 부분과, 조회 메서드가 `ChatRoomService`에 흡수된 부분이다.

다음에 할 일
- 수정이 필요하면 `ChatRoomSubscribeStrategy`의 `ChatRoomUseCase`를 지연 조회로 변경하는 최소 수정부터 검토한다.

이번 세션 추가 분석
- Spring AOP 기반 `@Async`는 프록시를 통해 외부에서 호출되는 메서드에 적용되며, private 메서드는 프록시가 가로챌 수 없어 비동기로 실행되지 않는다고 정리했다.
- 같은 클래스 내부에서 `this.privateMethod()` 또는 `this.publicAsyncMethod()`처럼 자기 자신을 호출하는 경우도 프록시를 거치지 않아 `@Async`가 적용되지 않는다는 점을 함께 정리했다.

현재 진행 상태
- 코드 변경 없이 `@Async`와 private 메서드의 동작 원리를 설명하는 수준으로 분석을 완료했다.

다음에 할 일
- 실제 코드에 적용해야 한다면 `@Async` 대상 메서드를 별도 Spring Bean의 public/package-private 메서드로 분리하고, 다른 Bean에서 주입받아 호출하는 구조로 변경한다.

이번 세션 추가 작업
- 워커 서버 분리 이후 API 서버에 남은 제거 후보를 전체 탐색했다.
- 미사용 FCM 코드, RabbitMQ/Debezium/SMTP 설정 잔재, CDC 전환 이후 남은 Outbox 재시도 코드, Redis 알림 구독 처리 변경을 커밋 단위로 분리했다.
- `chore : 워커 분리 후 배포 설정 정리` 커밋에서 API 이미지명/배포 경로, docker compose, application 설정, env 예시, nginx 설정 삭제를 정리했다.
- `refactor : 미사용 FCM 코드 제거` 커밋에서 FCM 도메인/리포지토리/서비스/인프라 코드와 Firebase/okhttp 의존성을 제거했고, Firebase 제거로 사라진 전이 의존성 대신 JetBrains annotations 의존성을 명시했다.
- `refactor : CDC 전환 후 Outbox 재시도 코드 제거` 커밋에서 Outbox 상태/재시도/스케줄러 관련 코드를 제거하고 CDC 기반 outbox 저장 흐름만 남겼다.
- `fix : Redis 알림 구독 처리 정리` 커밋에서 Redis pub/sub 알림 메시지를 STOMP 전송 포트로 전달하도록 정리했다.
- 각 주요 커밋 전후 `./gradlew compileJava`로 컴파일을 확인했고 최종 컴파일도 통과했다.

현재 진행 상태
- 워커 서버 분리 후 API 서버 정리 작업은 4개 코드 커밋으로 분리되어 완료되었다.
- 남은 untracked 파일은 `loadtest/test.js`, `member_bulk.ndjson`, `member_bulk_fixed.ndjson`이며 이번 커밋 범위에서는 제외했다.

다음에 할 일
- `NotificationTestDto` 이름이 임시 성격이므로 워커가 발행하는 Redis 메시지 계약과 맞춰 `RedisNotificationMessage` 같은 명확한 이름으로 후속 정리한다.
- 필요하면 `FcmTokenDto`와 FCM 관련 주석도 완전히 제거해 코드 표면을 더 줄인다.
- untracked 부하테스트 스크립트와 대용량 ndjson 파일은 보관, 이동, gitignore 추가 중 하나로 별도 판단한다.
